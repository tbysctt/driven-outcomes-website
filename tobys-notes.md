# Theme directory (wp-content/themes/driven-outcomes)
composer install

npm install
npm run dev

# Project root
docker compose up


  To Change Colors Later

  1. Edit wp-content/themes/driven-outcomes/resources/css/theme.css
  2. Modify the hex values or swap the semantic mappings
  3. Run npm run build in the theme directory

